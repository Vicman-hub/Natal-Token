# TODO

- [x] Update `src/config/chains.ts` to use `import.meta.env.VITE_*` for `projectId`, `USDT_BSC`, and `PRESALE_CONTRACT`.
- [x] Update `src/pages/Presale.tsx` to remove local hard-coded `PRESALE_CONTRACT` and import it from `src/config/chains.ts`.
- [x] Run `npm run build` (or the closest available script) to verify TypeScript/build passes.



